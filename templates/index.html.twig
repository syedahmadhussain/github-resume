{% extends 'base.html.twig' %}
{% block title %} Git Resume {% endblock %}
{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class=" offset-3 col-6 ">
                <h3 class="text-center text-danger">Create resume via Git</h3>
                <h6 class="text-center text-danger">Enter your git username</h6>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}